global:
  enabled: true

server:
  image:
    repository: "vault"
    tag: "1.3.1"

  service:
    enabled: true

  dev:
    enabled: true

  dataStorage:
    enabled: true
    size: 10Gi
    storageClass: null
    accessMode: ReadWriteOnce

annotations: |
  "vault/service-account1": "vault-auth"
